# 1. Count the number of upper and lower case letters in a String.
s = "Hello World"
upper = sum(1 for c in s if c.isupper())
lower = sum(1 for c in s if c.islower())
print(upper, lower)

# 2. Check whether a given String is Palindrome or not.
s = "madam"
print(s == s[::-1])

# 3. Return new string made of n copies of the first 2 chars of the original string.
s = "Wipro"
n = len(s)
print(s[:2] * n)

# 4. If the first or last character is 'x', return string without those characters.
s = "xHix"
if s[0] == 'x':
    s = s[1:]
if s and s[-1] == 'x':
    s = s[:-1]
print(s)

# 5. Return a string made of n repetitions of the last n characters.
s = "Wipro"
n = 3
print(s[-n:] * n)
